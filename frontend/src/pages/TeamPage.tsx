import {useEffect,useState} from 'react'
import {api,formatDateTime,money} from '../lib/api'
import {Modal} from '../components/Modal'

type E={id:string;display_name:string;position:string;revenue_today_cents:number;revenue_week_cents:number;revenue_month_cents:number;units_week:number;units_target:number;units_missing:number;units_percent:number;appointments_week:number;sales_week:number;presentations_week:number;active_rentals:number}
type Detail={dashboard:any;sales:any[];appointments:any[];rentals:any[]}
export function TeamPage(){const [rows,setRows]=useState<E[]>([]);const [selected,setSelected]=useState<E|null>(null);const [detail,setDetail]=useState<Detail|null>(null);useEffect(()=>{api<E[]>('/team/employees').then(setRows)},[]);async function open(e:E){setSelected(e);setDetail(null);const [dashboard,sales,appointments,rentals]=await Promise.all([api(`/dashboard?employee_id=${e.id}`),api<any[]>(`/sales?employee_id=${e.id}`),api<any[]>(`/appointments?employee_id=${e.id}`),api<any[]>(`/rentals?employee_id=${e.id}`)]);setDetail({dashboard,sales,appointments,rentals})}return <div className="page"><div className="page-head"><div><h1>Mitarbeiterübersicht</h1><p>Teamleistung je Mitarbeiter. Eine Zeile öffnet die Detailansicht.</p></div></div><div className="table-card wide"><table><thead><tr><th>Mitarbeiter</th><th>Wochenumsatz</th><th>Monat</th><th>Einheiten</th><th>Ziel</th><th>Termine</th><th>Verkäufe</th><th>Vorstellungen</th><th>Verleih</th></tr></thead><tbody>{rows.map(e=><tr className="clickable" key={e.id} onClick={()=>open(e)}><td><strong>{e.display_name}</strong><small>{e.position}</small></td><td>{money(e.revenue_week_cents)}</td><td>{money(e.revenue_month_cents)}</td><td>{e.units_week}</td><td><div className="mini-progress"><span style={{width:`${Math.min(e.units_percent,100)}%`}}/></div>{e.units_percent}%</td><td>{e.appointments_week}</td><td>{e.sales_week}</td><td>{e.presentations_week}</td><td>{e.active_rentals}</td></tr>)}</tbody></table></div>{selected&&<Modal title={`Mitarbeiter · ${selected.display_name}`} onClose={()=>{setSelected(null);setDetail(null)}}>{detail?<div className="team-detail"><div className="stat-grid"><div className="stat-card green"><small>Woche</small><strong>{money(selected.revenue_week_cents)}</strong></div><div className="stat-card"><small>Einheiten</small><strong>{selected.units_week}</strong></div><div className="stat-card"><small>Termine</small><strong>{selected.appointments_week}</strong></div><div className="stat-card"><small>Verleih</small><strong>{selected.active_rentals}</strong></div></div><h3>Nächster Termin</h3><p>{detail.dashboard.next_appointment?`${detail.dashboard.next_appointment.customer_name} · ${formatDateTime(detail.dashboard.next_appointment.start_at)}`:'Kein kommender Termin'}</p><h3>Letzte Verkäufe</h3>{detail.sales.slice(0,5).map(s=><div className="list-row" key={s.id}><span>{s.customer_name}<small>{formatDateTime(s.sold_at)}</small></span><strong>{money(s.total_cents)}</strong></div>)}<h3>Aktive Verleihgeräte</h3>{detail.rentals.filter(r=>r.status!=='returned').slice(0,5).map(r=><div className="list-row" key={r.id}><span>{r.product_name} · {r.customer_name}</span><small>{formatDateTime(r.due_at)}</small></div>)}</div>:<div className="loading">Details werden geladen…</div>}</Modal>}</div>}
