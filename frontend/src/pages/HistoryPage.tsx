import {useEffect,useState} from 'react'
import {api,formatDateTime,money} from '../lib/api'

type Row={customer_id:string;name:string;address:string;phone:string|null;email:string|null;purchased_products:{name:string;quantity:number;unit_price_cents:number;sold_at:string}[]}
export function HistoryPage(){const [rows,setRows]=useState<Row[]>([]);useEffect(()=>{api<Row[]>('/history').then(setRows)},[]);return <div className="page"><div className="page-head"><div><h1>Verlauf</h1><p>Kundendaten und relevante Verkaufsinformationen.</p></div></div><div className="history-grid">{rows.map(r=><section className="card" key={r.customer_id}><h3>{r.name}</h3><p>{r.address||'Keine Adresse hinterlegt'}</p><small>{r.phone||'–'} · {r.email||'–'}</small><div className="divider"/><strong>Gekauft</strong>{r.purchased_products.length?r.purchased_products.map((p,i)=><div className="list-row" key={i}><span>{p.quantity}× {p.name}<small>{formatDateTime(p.sold_at)}</small></span><strong>{money(p.unit_price_cents*p.quantity)}</strong></div>):<p>Keine Verkäufe hinterlegt.</p>}</section>)}</div>{!rows.length&&<div className="empty card">Noch keine relevante Kundenhistorie vorhanden.</div>}</div>}
